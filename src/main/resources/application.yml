spring:
  application:
    name: guesscolleague

  datasource:
    url: jdbc:postgresql://dpg-d7llo4nlk1mc73bahkk0-a.frankfurt-postgres.render.com:5432/guess_colleague
    username: admin
    password: 4bPYQ68hKPGaBJfVrB7PoAuSGEbBTBDJ
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: false

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:YOUR_BOT_TOKEN_HERE}
    username: ${TELEGRAM_BOT_USERNAME:GuessColleagueBot}

server:
  port: ${PORT:8080}

logging:
  level:
    com.wft.guesscolleague: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
